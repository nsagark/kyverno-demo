apiVersion: v1
kind: Pod
metadata:
  name: ghost-with-readonly-rootfilesystem
  namespace: random
spec:
  containers:
  - name: ghost
    image: ghost
    securityContext:
      readOnlyRootFilesystem: false
